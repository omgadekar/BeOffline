<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ======================================================================
         PERMISSIONS
         ====================================================================== -->

    <!-- Core VPN permission — Android will show the system VPN consent dialog -->
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE" />

    <!-- Scheduling: receive broadcast when device boots to restart active rules -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Foreground service for the VPN (required Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />

    <!-- Needed to read/write VPN state in background -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification for the persistent VPN status notification (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Wake locks to prevent WorkManager tasks from being killed -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Open-block engine: full-screen block overlay shown over a blocked app.
         Granted by the user via Settings (Display over other apps). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Narrow package visibility for the app picker: only launcher apps. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".BeOfflineApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeOffline">

        <!-- ================================================================
             MAIN ACTIVITY
             ================================================================ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.BeOffline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ================================================================
             CORE VPN SERVICE
             The android.permission.BIND_VPN_SERVICE ensures only the OS can
             bind to this service. The service handles all packet routing.
             ================================================================ -->
        <service
            android:name=".vpn.BeOfflineVpnService"
            android:exported="false"
            android:label="BeOffline VPN"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="systemExempted">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- ================================================================
             OPEN-BLOCK ACCESSIBILITY SERVICE
             Detects when a blocked app comes to the foreground during a
             restricted window. Bound by the system only; user must enable it
             in Accessibility settings (after the in-app prominent disclosure).
             ================================================================ -->
        <service
            android:name=".openblock.OpenBlockAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Accountability push (approvals, requests, tamper alerts). -->
        <service
            android:name=".accountability.AccountabilityMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".notifications.BlockedAppNotificationListenerService"
            android:exported="false"
            android:label="BeOffline notification control"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ================================================================
             BOOT RECEIVER
             Restarts the VPN when the device reboots if a rule was active.
             ================================================================ -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ================================================================
             SCHEDULE TRIGGER RECEIVER
             WorkManager alarm for schedule start/end events.
             ================================================================ -->
        <receiver
            android:name=".receiver.ScheduleAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.VpnRecoveryReceiver"
            android:exported="false" />

        <!-- Open-block schedule/timer alarms (mirrors ScheduleAlarmReceiver). -->
        <receiver
            android:name=".receiver.OpenBlockAlarmReceiver"
            android:exported="false" />

        <!-- Remove the default WorkManager initializer because BeOfflineApp provides
             the configuration and worker factory explicitly. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
